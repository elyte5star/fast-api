Starting the project

- Project Setup:

  - Clone project with

  ```
  git clone git@github.com:elyte5star/fast-api.git
  ```

- Docker setup run:

  - Run migrations with

  ```
  docker-compose run app
  ```

  - start server with

  ```
  docker-compose up
  ```
